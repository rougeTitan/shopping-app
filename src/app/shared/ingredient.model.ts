export class Ingredient {

    //Quickest way to create class which handles the field declaration and assignment
    //through construction only
    constructor(public name: string, public amount: number) {}
  }
  